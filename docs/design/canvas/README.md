# Canvas Claude Design — MemoryLane

Sources des artboards du canvas de design (maquettes + design system) :
https://claude.ai/code/artifact/6d402e65-0ce9-48cc-a8fa-43a43fae2235

- `Main.dc.html` — design system (tokens réels de `backend/resources/css/app.css`, light + dark)
- `Accueil / MesPhotos / Album / FicheMedia / FichePersonne` — maquettes fidèles de l'existant
- `AlbumHeroPhoto / AlbumHeroCarte` — propositions v2 de la page album (héro + galerie mosaïque)
- `canvas.json` — disposition des artboards

Le canvas publié est ré-assemblé depuis ces fichiers (skill `design` de Claude Code).
Si le canvas a été édité et sauvegardé dans l'interface, la vérité est côté artifact
(ré-extraire avant de retoucher ici).
